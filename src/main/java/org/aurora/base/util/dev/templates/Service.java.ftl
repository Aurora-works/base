package org.aurora.${projectName}.service.${package1};

import org.aurora.${projectName}.entity.${package1}.${entityName};
import org.aurora.${projectName}.service.BaseService;

public interface ${entityName}Service extends BaseService<${entityName}> {
}
