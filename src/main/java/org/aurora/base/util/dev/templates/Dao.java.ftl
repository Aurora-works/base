package org.aurora.${projectName}.dao.${package1};

import org.aurora.${projectName}.dao.BaseDao;
import org.aurora.${projectName}.entity.${package1}.${entityName};

public interface ${entityName}Dao extends BaseDao<${entityName}> {
}
